SELECT * FROM PROFESSOR;

-- null 값과의 산술 연산 결과는 null 이다
-- TODO

	
-- nvl() 함수
nvl(칼럼, 치환값)
칼럼 값이 null 이면 치환값 리턴

-- TODO
	
	
-- #4201
-- Professor 테이블 : 
-- 101번 학과 교수들의 이름(name), 급여(pay), 보너스(bonus ), 연봉(pay) 출력하기.
-- 연봉은 pay * 12 + bonus 로 계산,  bonus 가 없는 교수는 0 으로 계산	



--#4202 	
--professor 테이블 :  교수의 이름과 pay, bonus, 연봉 출력,  
--연봉은 pay*12 + bonus 로 계산, bonus 가 없으면 bonus를 0 으로 처리 (nvl2 사용)









